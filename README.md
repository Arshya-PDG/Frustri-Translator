# Frustri-Translator
This tool automates large-scale translation by leveraging the Gemini API. It features a UI-driven segmentation system that breaks down extensive texts or books into user-defined word counts. The application executes a sequential batch-processing workflow, programmatically sending chunks to the AI with optimized prompts to maintain consistency.
